/** @type {import('next').NextConfig} */
const nextConfig = {
  // Output: 'export' would be for purely static, but we need the API route.
  // For Capacitor, we can use a server URL or host the API elsewhere.
  // We'll keep it standard for now.
};

export default nextConfig;
